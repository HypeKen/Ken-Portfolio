import 'package:expensecalculator/index.dart';

class NewTransaction extends StatelessWidget {
  const NewTransaction({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    return Container();
  }
}
